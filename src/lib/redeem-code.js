/**
 * Redeem Code type definitions (JavaScript documentation)
 * 
 * CodeStatus: 'active' | 'expired'
 * 
 * RedeemCode: {
 *   id: string
 *   code: string
 *   reward: string
 *   status: CodeStatus
 *   expiresAt: string | null
 *   addedAt: string
 *   source: string | null
 * }
 * 
 * StatusFilter: 'all' | 'active' | 'expired'
 */

// No exports needed - this is just documentation for plain JS
